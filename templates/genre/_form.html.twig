{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-right mt-5">
        <a href="{{ path('genre_index') }}" class="btn btn-outline-primary">Annuler</a>
        <button class="btn btn-primary">{{ button_label|default('form.btnSave'|trans) }}</button>
    </div>
{{ form_end(form) }}
